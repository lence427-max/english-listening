# 2026-06-11 开发日志

## Explore Mode 稳定性修复

- 修复 `pathHistory` 未声明导致 Explore Mode 永久显示“探索中”的问题。
- 增加最新请求协调器：连续搜索时自动终止旧请求，只允许最后一次搜索更新界面。
- `fetchMindMap()` 支持外部取消信号，并保留 10 秒请求超时。
- 修复动画取消后 Promise 永不结束的问题，避免旧加载流程悬挂。
- 安全定时器绑定具体请求，旧请求不会误改当前中心节点。
- 主动取消 AI 请求时不再输出误导性的失败警告。

## 通用稳定性优化

- 动态模块加载增加统一错误处理，加载失败时显示可恢复的页面提示。
- 素材管理中的“待复习”统计改为读取真实训练记录，并按素材去重。

## 自动测试

- 新增最新请求协调器测试。
- 新增 Explore 状态声明回归测试。
- 新增动态视图加载测试。
- 新增待复习素材统计测试。
- 浏览器完成快速连续搜索 `team → leadership → trust` 回归测试。

## 核心学习链路回归保护

- 为 LCS 听写对比增加大小写、标点、漏词、多词和替换词测试。
- 为段落拆分和音频时间估算增加连续性测试。
- 抽离全文训练完成逻辑，统一保存听写结果和最近 20 次成绩。
- 修复全文听写完成后未创建训练记录、无法进入智能复习的问题。
- 智能复习兼容当前的整篇 `dictationResult`、段落 `paragraphResults` 和旧版句子数据。
- 新增 `npm test` 与 `npm run check` 作为统一验证命令。
- Windows PowerShell 执行策略可能拦截 `npm.ps1`，可使用 `npm.cmd test` 与 `npm.cmd run check`。

## 浏览器验证限制

- 当前内置素材没有本地 `audioId`，训练页会正确显示“还没有可训练的素材”。
- 为避免篡改用户数据，本轮浏览器测试验证了训练空状态；提交训练到复习的闭环由纯逻辑自动测试覆盖。

## 最终验证

- `npm.cmd test`：15 项测试全部通过，0 失败。
- `npm.cmd run check`：所有 JavaScript 文件语法检查通过。
- `git diff --check`：无空白错误，仅有现有文件的 LF/CRLF 转换提示。
- 应用内浏览器：Explore 连续搜索无竞态错误；训练空状态正常；控制台无应用错误。

## 当前状态

- 任务于 2026-06-11 暂停，网站保持本地开发状态。
- 本轮改动尚未提交 Git；工作区包含此前 Explore V4/V5 及本轮稳定性、测试相关修改。
- 下次继续时优先进行 `innerHTML` 安全审计，或补充带本地音频素材的完整浏览器端训练闭环测试。

## 后续建议

- 扩大核心训练链路的自动测试覆盖。
- 审计用户输入和 API 数据相关的 `innerHTML` 写入。
- 补充移动端和数据导入导出的浏览器回归测试。
